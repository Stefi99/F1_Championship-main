# Beispiel-Konfigurationsdatei
# Kopiere diese Datei zu application.properties und fülle die Werte aus
# ODER verwende Environment Variables (empfohlen für Produktion)

spring.application.name=f1-Championship

# Database Configuration
# Setze diese Environment Variables:
# DB_URL=jdbc:postgresql://localhost:5432/f1_championship
# DB_USERNAME=postgres
# DB_PASSWORD=dein_db_passwort
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/f1_championship}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:}

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080

# JWT Configuration
# Setze diese Environment Variable:
# JWT_SECRET=dein_sehr_langes_geheimes_token_mindestens_32_zeichen_lang
# WICHTIG: Das Secret muss mindestens 32 Zeichen (256 Bit) lang sein für HS256
jwt.secret=${JWT_SECRET:}

